node {
  stage('HelloWorld') {
    sh "echo \$(ls)"
    sh "pwd"
    sh "cd test && kubectl apply  --context minikube -f ."
    sh "pwd"
    sh "kubectl apply --context minikube -f redis.yaml"

  }

}